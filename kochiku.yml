test_command: 'scripts/ci-build.sh'

log_file_globs:
  - "**/hs_err_pid*.log"
  - "java-*.hprof"
  - "**/build/reports/lint-results.html"
  - "**/build/reports/lint-results*.txt"

targets:
  - type: buildDebug
    write_to_cache:
      - sample-app/build/outputs/apk/debug/*.apk

  - type: publishDebug
    requires:
      - buildDebug
    read_from_cache:
      - sample-app/build/outputs/apk/debug/*.apk

  - type: paykit:lint
