pluginManagement {
  repositories {
    gradlePluginPortal()
    google()
    mavenCentral()
  }
}
dependencyResolutionManagement {
  repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
  repositories {
    google()
    mavenCentral()
  }
}
rootProject.name = "Pay Kit SDK"
include ':core'
include ':analytics-core'
include ':logging'
include ':PayKitLib'

//enableFeaturePreview("GRADLE_METADATA")

includeBuild("../cash-mobile-paykit-kmp"){
  dependencySubstitution {
    substitute(module("app.cash.paykit.state:paykit")).using(project(":PayKitLib"))
  }
}
